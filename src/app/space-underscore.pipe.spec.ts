import { SpaceUnderscorePipe } from './space-underscore.pipe';

describe('SpaceUnderscorePipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceUnderscorePipe();
    expect(pipe).toBeTruthy();
  });
});
